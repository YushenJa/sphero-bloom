🚗 Движение (Movement)
Методы для управления моторами и перемещением.

roll(heading, speed, duration): Заставляет робота катиться. (Направление 0-360, скорость 0-255, время в сек).

stop_roll(): Полная остановка моторов.

spin(angle, time): Вращение на месте на заданный угол за заданное время.

set_speed(speed): Установка скорости (без изменения направления).

get_speed(): Узнать текущую скорость.

set_heading(heading): Повернуть робота (изменить "перед"), не начиная движение.

get_heading(): Узнать, куда сейчас смотрит робот.

raw_motor(left_mode, left_speed, right_mode, right_speed): Прямое управление левым и правым мотором (для продвинутых).

reset_aim(): Сбросить направление (текущее положение становится "0 градусов").

set_stabilization(bool): Включить/выключить гироскоп (стабилизацию).

calibrate_compass(): Откалибровать компас (робот покрутится).

set_compass_direction(direction): Принудительно задать направление компаса.

get_compass_direction(): Получить данные компаса.

get_velocity(): Получить текущую скорость по осям X/Y.

set_waddle(bool): (Для спец. игрушек) Включить режим "ковыляния" (как у пингвина).

set_stance(stance): (Для R2-D2) Изменить стойку (две ноги / три ноги).




 

💡 Свет и Матрица 8x8 (Lights & Matrix)
Самые важные методы для Sphero Bolt.


Матрица (Экран 8x8):

clear_matrix(): Выключить все пиксели матрицы.

set_matrix_pixel(x, y, color): Зажечь один пиксель (x: 0-7, y: 0-7).

set_matrix_line(x1, y1, x2, y2, color): Нарисовать линию.

set_matrix_fill(x1, y1, x2, y2, color): Залить прямоугольную область цветом.

set_matrix_character(char, color): Показать одну букву/символ.

set_matrix_rotation(rotation): Повернуть изображение на матрице (0, 90, 180, 270 градусов).

scroll_matrix_text(text, color, fps, wait): Прокрутить бегущую строку текста.

play_matrix_animation(frame_index): Запустить встроенную анимацию.

pause_matrix_animation(): Пауза анимации.

resume_matrix_animation(): Продолжить анимацию.

register_matrix_animation(...): Загрузить свою кастомную анимацию.

override_matrix_animation_framerate(...): Изменить скорость анимации.

override_matrix_animation_transition(...): Изменить плавность переходов.




Светодиоды (LEDs):

set_main_led(color): Основной цвет (подсветка матрицы).

get_main_led(): Узнать текущий цвет.

set_back_led(brightness): Задний синий светодиод (0-255).

get_back_led(): Узнать яркость заднего диода.

set_front_led(color): Передний светодиод.

get_front_led(): Узнать цвет переднего диода.

fade(start_color, end_color, duration): Плавное изменение цвета.

strobe(color, period, count): Стробоскоп (мигание).

get_color(): Получить текущий общий цвет.

(Методы ниже в основном для дроидов R2-D2 или RVR, но могут быть в списке):

set_dome_leds, get_dome_leds

set_holo_projector_led, get_holo_projector_led

set_logic_display_leds, get_logic_display_leds

set_left_headlight_led, set_right_headlight_led

set_left_led, set_right_led







📊 Сенсоры (Sensors)
Получение данных от робота.

get_acceleration(): Данные акселерометра (сила перегрузки по X, Y, Z). Используется для "тряски".

get_vertical_acceleration(): Ускорение только по вертикали.

get_gyroscope(): Данные гироскопа (скорость вращения по осям X, Y, Z).

get_orientation(): Ориентация в пространстве (наклон, крен, рыскание).

get_location(): Координаты X/Y относительно точки старта.

get_distance(): Общая пройденная дистанция.

get_luminosity(): Датчик света (яркость окружающей среды).

get_luminosity_direct(): Прямое значение с датчика света (сырые данные).

listen_for_color_sensor(...): (Обычно для RVR) Слушать датчик цвета пола.






🔊 Звук (Sound)
play_sound(sound_enum): Воспроизвести встроенный звук (нужен список spherov2.types.Sound).






📡 Инфракрасная связь (IR Communication)
Для общения роботов друг с другом.

start_ir_broadcast(far_code, near_code): Начало вещания сигнала (я здесь!).

stop_ir_broadcast(): Остановка вещания.

send_ir_message(message): Отправить сообщение другому роботу.

get_last_ir_message(): Прочитать последнее полученное сообщение.

listen_for_ir_message(...): Начать слушать сообщения.

start_ir_follow(...): Следовать за другим роботом (IR).

stop_ir_follow(): Перестать следовать.

start_ir_evade(...): Убегать от другого робота.

stop_ir_evade(): Перестать убегать.






⚙️ Система и События (System & Events)
register_event(event_type, listener): Привязать функцию к событию (например, при столкновении).

play_animation(animation_id): Проиграть движение/звук (общие анимации).